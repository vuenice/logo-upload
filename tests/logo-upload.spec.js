import { describe, it, expect } from 'vitest';
import VueNiceLogoUpload from '../src/VueNiceLogoUpload.vue';

describe('VueNiceLogoUpload', () => {
  it('is defined', () => {
    expect(VueNiceLogoUpload).toBeDefined();
  });
});
